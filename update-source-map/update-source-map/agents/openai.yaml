interface:
  display_name: "Update Source Map"
  short_description: "Create or update agent source map"
  default_prompt: "Use $update-source-map to create or update the agent-readable source map in this workspace. Detect existing first; create if missing, update if present. Always report the plan before executing."
