interface:
  display_name: "MiniMax Thorough Execution"
  short_description: "Execute the user's prompt strictly with organized temp artifacts, skill delegation, and source links"
  default_prompt: "Use $minimax-thorough-execution to execute my prompt exactly as written, without rewriting it, shrinking scope, scattering temp artifacts, skipping skill discovery, guessing from text instead of inspecting images, or omitting source links."